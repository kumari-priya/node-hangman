import Word from 'word';

class Game extends Word{
  constructor(word,letter,attempts,reset) {
    this.words = ['SQL','Javasript','JQuery','Bootstrap','Ruby on Rails','Python Django','Angular','Node','React'];
    this.index = 0;
    this.letter = letter;
    this.attempts = attempts;
    this.reset = reset;
  }
  checkLetter(){


  }
  nextWord(){


  }
}
